<Window x:Class="Ga144.Evb.Ide.Views.NewCProjectWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="New C project"
        Width="480"
        SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
  <Grid Margin="16">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>

    <TextBlock Grid.Row="0" Text="Name" FontWeight="SemiBold" />
    <TextBox Grid.Row="1" Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" />

    <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,8,0,8">
      <TextBlock Text="Kind:" VerticalAlignment="Center" Margin="0,0,8,0" />
      <RadioButton Content="Program" IsChecked="{Binding IsProgramKind, Mode=TwoWay}" VerticalAlignment="Center" Margin="0,0,16,0" />
      <RadioButton Content="Library" IsChecked="{Binding IsLibraryKind, Mode=TwoWay}" VerticalAlignment="Center" />
    </StackPanel>

    <TextBlock Grid.Row="3"
                           Text="{Binding ResolvedRootPath, StringFormat='Will be created at: {0}'}"
                           TextWrapping="Wrap"
                           Foreground="#555"
                           Margin="0,0,0,4" />

    <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
      <Button Content="Cancel" IsCancel="True" MinWidth="90" Margin="0,0,8,0" />
      <Button Content="Create" IsDefault="True" Click="OnCreateClick" MinWidth="90" />
    </StackPanel>
  </Grid>
</Window>