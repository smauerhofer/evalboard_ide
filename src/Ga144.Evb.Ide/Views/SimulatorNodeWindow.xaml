<Window x:Class="Ga144.Evb.Ide.Views.SimulatorNodeWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding CoordinateText, StringFormat='Node {0} simulator'}"
        Width="980"
        Height="800"
        MinWidth="680"
        MinHeight="520"
        WindowStartupLocation="CenterOwner">
  <Grid Margin="10">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>

    <Grid Margin="0,0,0,8">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*" />
        <ColumnDefinition Width="Auto" />
      </Grid.ColumnDefinitions>

      <StackPanel Grid.Column="0" Orientation="Horizontal">
        <TextBlock Text="{Binding CoordinateText, StringFormat='Node {0}'}" FontSize="20" FontWeight="SemiBold" VerticalAlignment="Center" />
        <TextBlock Text="{Binding StatusText}" Margin="12,0,0,0" VerticalAlignment="Center" FontStyle="Italic" Foreground="#555" />
      </StackPanel>

      <Button Grid.Column="1" Content="Copy registers &amp; stacks" Padding="10,4" VerticalAlignment="Center"
              Click="OnCopyRegistersAndStacksClick"
              ToolTip="Copies this node's T/S/R/A/B/P/IO/Carry registers and both stacks (top first), exactly as shown below, as plain text." />
    </Grid>

    <TextBlock Grid.Row="1"
               Foreground="#B33A3A"
               Margin="0,0,0,8"
               TextWrapping="Wrap"
               Text="{Binding ErrorText}"
               Visibility="{Binding ErrorVisibility}" />

    <Border Grid.Row="2" Background="#EEF4EF" BorderBrush="#C6D5C9" BorderThickness="1" Padding="8" Margin="0,0,0,8">
      <Grid>
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="Auto" />
          <ColumnDefinition Width="Auto" />
          <ColumnDefinition Width="*" />
          <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="0" Margin="0,0,24,0">
          <TextBlock FontWeight="SemiBold" Text="Registers" />
          <TextBlock FontFamily="Consolas" Text="{Binding AText, StringFormat='A:  {0}'}" />
          <TextBlock FontFamily="Consolas" Text="{Binding BText, StringFormat='B:  {0}'}" />
          <TextBlock FontFamily="Consolas" Text="{Binding PText, StringFormat='P:  {0}'}"
                     ToolTip="Current position -- the address of the word this node is stepping through slot-by-slot (also highlighted in the RAM/ROM listing below)." />
          <TextBlock FontFamily="Consolas" Text="{Binding IoText, StringFormat='IO: {0}'}" />
          <TextBlock FontFamily="Consolas" Text="{Binding CarryText, StringFormat='Carry: {0}'}" />
        </StackPanel>

        <StackPanel Grid.Column="1" Margin="0,0,24,0">
          <TextBlock FontWeight="SemiBold" Text="Current instruction" />
          <TextBlock FontFamily="Consolas" Text="{Binding CurrentInstructionText}" TextWrapping="Wrap" MaxWidth="220" />
        </StackPanel>

        <StackPanel Grid.Column="2" Margin="0,0,24,0">
          <TextBlock FontWeight="SemiBold" Text="Parameter stack (top first: T, S, ...)" />
          <ItemsControl ItemsSource="{Binding ParameterStackRows}">
            <ItemsControl.ItemTemplate>
              <DataTemplate>
                <TextBlock FontFamily="Consolas">
                  <Run Text="{Binding Label, Mode=OneWay}" FontWeight="SemiBold" />
                  <Run Text=": " />
                  <Run Text="{Binding ValueText, Mode=OneWay}" />
                </TextBlock>
              </DataTemplate>
            </ItemsControl.ItemTemplate>
          </ItemsControl>
        </StackPanel>

        <StackPanel Grid.Column="3">
          <TextBlock FontWeight="SemiBold" Text="Return stack (top first: R, ...)" />
          <ItemsControl ItemsSource="{Binding ReturnStackRows}">
            <ItemsControl.ItemTemplate>
              <DataTemplate>
                <TextBlock FontFamily="Consolas">
                  <Run Text="{Binding Label, Mode=OneWay}" FontWeight="SemiBold" />
                  <Run Text=": " />
                  <Run Text="{Binding ValueText, Mode=OneWay}" />
                </TextBlock>
              </DataTemplate>
            </ItemsControl.ItemTemplate>
          </ItemsControl>
        </StackPanel>
      </Grid>
    </Border>

    <Border Grid.Row="3" Background="White" BorderBrush="#C6D5C9" BorderThickness="1" Padding="8" Margin="0,0,0,8">
      <StackPanel>
        <TextBlock FontWeight="SemiBold" Text="Ports" Margin="0,0,0,4" />
        <ItemsControl ItemsSource="{Binding PortRows}">
          <ItemsControl.ItemsPanel>
            <ItemsPanelTemplate>
              <UniformGrid Columns="4" />
            </ItemsPanelTemplate>
          </ItemsControl.ItemsPanel>
          <ItemsControl.ItemTemplate>
            <DataTemplate>
              <StackPanel Orientation="Horizontal" Margin="0,0,12,0">
                <TextBlock Text="{Binding DirectionText}" FontWeight="SemiBold" Width="60" />
                <TextBlock Text="&#8592;" FontWeight="Bold" Foreground="#1565C0" Margin="0,0,2,0"
                           Visibility="{Binding InboundVisibility}" ToolTip="Stalled reading via this port" />
                <TextBlock Text="&#8594;" FontWeight="Bold" Foreground="#B33A3A" Margin="0,0,2,0"
                           Visibility="{Binding OutboundVisibility}" ToolTip="Stalled writing via this port" />
                <TextBlock Text="{Binding TargetText}" Margin="0,0,4,0" />
                <TextBlock Text="{Binding ValueText}" FontFamily="Consolas" />
              </StackPanel>
            </DataTemplate>
          </ItemsControl.ItemTemplate>
        </ItemsControl>
      </StackPanel>
    </Border>

    <TabControl Grid.Row="4">
      <TabItem Header="RAM">
        <DockPanel>
          <TextBlock DockPanel.Dock="Top"
                     Margin="0,4,0,4"
                     Foreground="#555"
                     TextWrapping="Wrap"
                     Text="The highlighted row is this node's current position P -- the word it is stepping through slot-by-slot." />
          <ListView ItemsSource="{Binding RamRows}">
            <ListView.ItemContainerStyle>
              <Style TargetType="ListViewItem">
                <Style.Triggers>
                  <DataTrigger Binding="{Binding IsCurrentPosition}" Value="True">
                    <Setter Property="Background" Value="#FFF3B0" />
                    <Setter Property="FontWeight" Value="Bold" />
                  </DataTrigger>
                </Style.Triggers>
              </Style>
            </ListView.ItemContainerStyle>
            <ListView.View>
              <GridView>
                <GridViewColumn Header="Address" DisplayMemberBinding="{Binding AddressHex}" Width="60" />
                <GridViewColumn Header="Label" DisplayMemberBinding="{Binding Label}" Width="120" />
                <GridViewColumn Header="Hex" DisplayMemberBinding="{Binding HexText}" Width="70" />
                <GridViewColumn Header="Disassembly" DisplayMemberBinding="{Binding Disassembly}" Width="420" />
              </GridView>
            </ListView.View>
          </ListView>
        </DockPanel>
      </TabItem>
      <TabItem Header="ROM">
        <DockPanel>
          <TextBlock DockPanel.Dock="Top"
                     Margin="0,4,0,4"
                     Foreground="#555"
                     TextWrapping="Wrap"
                     Text="The highlighted row is this node's current position P -- the word it is stepping through slot-by-slot." />
          <ListView ItemsSource="{Binding RomRows}">
            <ListView.ItemContainerStyle>
              <Style TargetType="ListViewItem">
                <Style.Triggers>
                  <DataTrigger Binding="{Binding IsCurrentPosition}" Value="True">
                    <Setter Property="Background" Value="#FFF3B0" />
                    <Setter Property="FontWeight" Value="Bold" />
                  </DataTrigger>
                </Style.Triggers>
              </Style>
            </ListView.ItemContainerStyle>
            <ListView.View>
              <GridView>
                <GridViewColumn Header="Address" DisplayMemberBinding="{Binding AddressHex}" Width="60" />
                <GridViewColumn Header="Label" DisplayMemberBinding="{Binding Label}" Width="120" />
                <GridViewColumn Header="Hex" DisplayMemberBinding="{Binding HexText}" Width="70" />
                <GridViewColumn Header="Disassembly" DisplayMemberBinding="{Binding Disassembly}" Width="420" />
              </GridView>
            </ListView.View>
          </ListView>
        </DockPanel>
      </TabItem>
    </TabControl>
  </Grid>
</Window>
